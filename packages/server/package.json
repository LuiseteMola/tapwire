{
  "name": "@tapwire/server",
  "version": "0.1.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist", "ui-dist"],
  "engines": { "node": ">=18.0.0" },
  "license": "MIT",
  "scripts": {
    "dev": "TAPWIRE_PORT=4000 tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@hono/node-server": "^1.14.1",
    "hono": "^4.7.10",
    "path-to-regexp": "^8.4.2"
  },
  "devDependencies": {
    "typescript": "*",
    "vitest": "*"
  }
}
